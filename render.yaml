services:
  - type: web
    name: network-swap-app
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "echo 'Serving static files from public/' && sleep infinity"
    envVars:
      - key: RACK_ENV
        value: production
    autoDeploy: false
