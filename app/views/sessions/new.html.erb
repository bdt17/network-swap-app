<!DOCTYPE html>
<html>
<head>
  <title>Network Swap - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-slate-900 via-indigo-900 to-purple-900 min-h-screen flex items-center justify-center p-8">
  <div class="max-w-md w-full bg-white/10 backdrop-blur-xl rounded-3xl shadow-2xl border border-white/20 p-8">
    <div class="text-center mb-8">
      <div class="w-24 h-24 bg-gradient-to-r from-emerald-500 to-teal-600 rounded-3xl flex items-center justify-center mx-auto mb-6 shadow-2xl">
        <span class="text-4xl">🌐</span>
      </div>
      <h1 class="text-4xl font-bold bg-gradient-to-r from-emerald-400 via-teal-400 to-blue-400 bg-clip-text text-transparent mb-2">
        Network Swap
      </h1>
      <p class="text-white/80 text-lg">Thomas IT Device Management</p>
    </div>

    <%= form_with url: "/session", local: true do |f| %>
      <div class="space-y-6">
        <div>
          <%= f.label :email, "Email", class: "block text-sm font-semibold text-white/90 mb-2" %>
          <%= f.email_field :email, required: true, autofocus: true, 
              class: "w-full px-5 py-4 bg-white/10 border border-white/20 rounded-2xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent transition-all" %>
        </div>
        
        <div>
          <%= f.label :password, "Password", class: "block text-sm font-semibold text-white/90 mb-2" %>
          <%= f.password_field :password, required: true, 
              class: "w-full px-5 py-4 bg-white/10 border border-white/20 rounded-2xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent transition-all" %>
        </div>
        
        <%= f.submit "Sign In", class: "w-full bg-gradient-to-r from-emerald-500 to-teal-600 hover:from-emerald-600 hover:to-teal-700 text-white font-bold py-4 px-6 rounded-2xl shadow-xl hover:shadow-2xl transition-all duration-300 text-lg uppercase tracking-wide" %>
      </div>
    <% end %>

    <div class="mt-6 text-center text-white/60 text-sm">
      First time? Use <code>admin@thomasit.com</code> / <code>password</code>
    </div>
  </div>
</body>
</html>
