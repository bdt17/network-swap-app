<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thomas IT Network Swap App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS CDN (Production ready) -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: { extend: { colors: { 'thomas-blue': '#1e40af', 'thomas-dark': '#111827' } } }
    }
  </script>
  
  <!-- Custom Thomas IT Styles -->
  <style>
    .gradient-bg { background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%); }
    .glass-card { backdrop-filter: blur(10px); background: rgba(255,255,255,0.1); }
  </style>
</head>
<body class="bg-gradient-to-br from-slate-900 via-blue-900 to-slate-900 min-h-screen text-white">
  
  <!-- Navigation -->
  <nav class="glass-card shadow-2xl sticky top-0 z-50 backdrop-blur-md border-b border-white/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <h1 class="text-2xl font-bold bg-gradient-to-r from-white to-blue-200 bg-clip-text text-transparent">
            Thomas IT Network Swap
          </h1>
        </div>
        <div class="flex items-center space-x-4">
          <%= link_to "Devices", devices_path, class: "px-4 py-2 rounded-lg hover:bg-white/20 transition-all" %>
          <%= link_to "Sites", sites_path, class: "px-4 py-2 rounded-lg hover:bg-white/20 transition-all" %>
          <%= link_to "Swaps", swaps_path, class: "px-4 py-2 rounded-lg hover:bg-white/20 transition-all" %>
          <% if user_signed_in? %>
            <span class="text-sm text-blue-200">👋 <%= current_user.email.split('@').first.capitalize %></span>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "px-4 py-2 rounded-lg hover:bg-red-500/30 transition-all" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "px-4 py-2 rounded-lg hover:bg-green-500/30 transition-all" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <main class="max-w-7xl mx-auto px-4 py-12 sm:px-6 lg:px-8">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="glass-card border-t border-white/20 mt-16 py-8">
    <div class="max-w-7xl mx-auto px-4 text-center text-sm text-blue-200">
      © 2026 Thomas Information Technology • Phase 4-7 LIVE • Rails 8.1 + PostgreSQL
    </div>
  </footer>
</body>
</html>
