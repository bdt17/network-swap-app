<!DOCTYPE html>
<html>
<head>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Thomas IT Network Swap</title>
</head>
<body class="bg-gray-900 text-white min-h-screen">
  <div class="container mx-auto px-4 py-12">
    <h1 class="text-6xl font-bold bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent mb-8">
      Network Swap Dashboard
    </h1>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <div class="bg-gray-800 p-8 rounded-xl shadow-2xl">
        <div class="text-5xl font-bold text-blue-400"><%= Device.count %></div>
        <div class="text-gray-400 mt-2">Devices</div>
      </div>
      <div class="bg-gray-800 p-8 rounded-xl shadow-2xl">
        <div class="text-5xl font-bold text-green-400"><%= Site.count %></div>
        <div class="text-gray-400 mt-2">Sites</div>
      </div>
      <div class="bg-gray-800 p-8 rounded-xl shadow-2xl">
        <div class="text-5xl font-bold text-orange-400">12</div>
        <div class="text-gray-400 mt-2">Pending Swaps</div>
      </div>
    </div>

    <div class="bg-gray-800 rounded-xl shadow-2xl overflow-hidden">
      <div class="bg-gray-700 px-6 py-4">
        <h2 class="text-2xl font-bold">Recent Devices</h2>
      </div>
      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead class="bg-gray-700">
            <tr>
              <th class="px-6 py-4 text-left font-bold">Device</th>
              <th class="px-6 py-4 text-left font-bold">Site</th>
              <th class="px-6 py-4 text-left font-bold">Status</th>
            </tr>
          </thead>
          <tbody>
            <% Device.limit(5).each do |d| %>
            <tr class="border-t border-gray-700 hover:bg-gray-700">
              <td class="px-6 py-4 font-mono"><%= d.name %></td>
              <td class="px-6 py-4"><%= d.site&.name || 'Unknown' %></td>
              <td>
                <span class="px-3 py-1 rounded-full text-xs font-bold <%= d.active? ? 'bg-green-500' : 'bg-red-500' %>">
                  <%= d.active? ? 'Active' : 'EOL' %>
                </span>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>
