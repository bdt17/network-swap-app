default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: network_swap_development

test:
  <<: *default
  database: network_swap_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
